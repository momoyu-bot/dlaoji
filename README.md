# 打捞机

从 Grok 导出的 `prod-grok-backend.json` 里，把做过的 HTML 小文件捞回来。

## 用法

1. 下载 [打捞机.html](./打捞机.html)（或 [dlaoji.html](./dlaoji.html)，内容相同）
2. 用浏览器打开这个 HTML
3. 把 `prod-grok-backend.json` 拖进去

文件不会上传，全部在本机浏览器里完成。
